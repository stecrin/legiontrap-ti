name: CI
permissions:
  contents: read

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install black==26.5.1 ruff pytest pytest-cov pytest-env fastapi httpx pydantic-settings
          pip install pip-audit bandit

      - name: Lint
        run: |
          black --check .
          ruff check .

      - name: Tests
        run: |
          pytest -q

      - name: Security — dependency audit
        run: pip-audit

      - name: Security — static analysis
        run: bandit -r app/ -ll
